// Unité de traduction dédiée à l'implémentation de stb_image (décodage PNG / JPG).
// Isolée dans sa propre TU, warnings coupés depuis le CMakeLists (code tiers).
#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>
